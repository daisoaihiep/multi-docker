name: Deploy MultiDocker
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
      - run: docker build -t batluongsoai/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true batluongsoai/react-test npm test

      - run: docker build -t batluongsoai/multi-client ./client
      - run: docker build -t batluongsoai/multi-nginx ./nginx
      - run: docker build -t batluongsoai/multi-server ./server
      - run: docker build -t batluongsoai/multi-worker ./worker

      - run: docker push batluongsoai/multi-client
      - run: docker push batluongsoai/multi-nginx
      - run: docker push batluongsoai/multi-server
      - run: docker push batluongsoai/multi-worker
